import React from "react";

const Logout = () => {
  // Perform logout logic here, e.g., clear authentication status

  return (
    <div>
      <h1>Logout</h1>
      <p>You have been logged out successfully.</p>
    </div>
  );
};

export default Logout;
